{
  // docs/ 下 plan/ 和 spec/ 是历史冻结快照（详见 rules/03_WORKSPACE.md）。
  // 用 files.exclude 将它们排除出资源管理器、文本搜索与语义索引，
  // 保持工作区整洁，避免历史文档干扰搜索。文件仍在 git 与磁盘中。
  // docs/adr/ 保持可见，方便查阅架构决策记录。
  "files.exclude": {
    "docs/plans": true,
    "docs/specs": true
  }
}
