---
name: brainstorming
description: "在开始实现前，把需求澄清成设计文档。适用于新增功能、修改行为、组件设计、接口设计、流程改造、架构取舍和多方案比较。只要用户还在说‘先想一下’、‘先给方案’、‘先规划一下’、‘先比较两种做法’，就优先使用这个 skill。即使任务看起来很简单，也先做简短设计。设计获批前，不要在本 skill 中写代码、生成补丁或进入实现。"
argument-hint: 描述要做的功能、约束、背景或已有想法；可留空
---

# Brainstorming Skill

这个 skill 用于把模糊想法收敛成经用户批准的设计文档，再把下一步明确交给 `writing-plans`。

这是一个 manual-first 的规划 skill，不是实现 skill。
在这个 skill 中，设计获批前不要写代码、不要生成补丁、不要进入实现。

## 何时使用

在以下场景优先使用这个 skill：
- 用户想新增功能、调整行为、设计组件、设计接口或改变系统流程
- 用户想先比较几种实现方案，再决定怎么做
- 用户只给了目标和约束，但还没有清晰设计
- 任务涉及多个文件、多个边界，或存在明显取舍
- 任务看起来很小，但仍需要先确认范围、成功标准或边界

## 不要路由到 brainstorming

以下请求默认不要使用这个 skill：
- 已经有批准过的设计文档，只是要写实施计划
- 直接实现、修 bug、改代码、跑测试或提交补丁
- 代码审查、缺陷分析、交接总结、状态同步
- 单纯解释某段代码、回答事实问题，或查找定义

## 硬约束

- 设计获批前，不要在本 skill 中写代码、生成补丁或切到实现流程
- 一次只问一个问题；如果需要继续追问，也要等用户回答后再问下一题
- 优先使用单选或多选问题；只有在必须时才使用开放题
- 先看当前项目上下文，再提设计建议；跟随现有模式，不做无关重构
- 如果某个问题用图、界面草图、布局对比或关系图会明显更清楚，只在那个具体问题出现时再提出可视化辅助；不要在开场默认提出
- 只有当前 runtime 确实有可用的浏览器、HTML 或绘图工具时，才承诺可视化辅助；工具不存在时，把原则转成文档里的 Mermaid、ASCII 图或文字结构
- 如果需求跨多个独立子系统，先拆分，再只处理第一个子项目
- 设计文档默认要落盘；路径优先级是：用户指定路径 > 仓库已有约定 > `docs/specs/`
- 写完设计文档后先做 inline 自检，再让用户审批
- CHECKPOINT A/B 必须要求用户给出明确确认令牌；不要把“看起来可以”“先这样”这类含糊认同当批准
- 这个 skill 的终态是进入 `writing-plans`，不是直接编码

## 🔴 CHECKPOINTS

- 🔴 CHECKPOINT A · 方案方向确认：范围与关键边界应在 PHASE 2-4 的前置问题里逐一确认完毕；只有在用户回复 `接受：推荐方案`、`接受：方案 A/B/C` 或同等明确确认后，才能进入 PHASE 5 写设计文档
- 🔴 CHECKPOINT B · 文档审批：只有在用户回复 `批准：<设计文档路径>` 或同等明确批准后，才能进入 PHASE 8 和 `writing-plans`
- 🛑 STOP · 错误路由：如果请求本质上是实现、修 bug、代码审查，或已有设计后的实施计划，立即停止本 skill，改走对应流程

模糊表述不算通过检查点，例如“看起来可以”“先这样”“应该行”“你继续吧”“好的”“继续吧”“都行”。遇到这些表述时，只发一个确认问题，要求用户改用上面的确认令牌。

判定“同等明确确认”：必须明确点名所选方案（如“方案 A”“推荐方案”）或使用确认令牌本身；只表达整体态度而未点名方案的，一律视为模糊。半角冒号与全角冒号等价。

判定 CHECKPOINT B 的“同等明确批准”：必须出现 `批准：<path>` 令牌，或清楚点明批准的是哪份设计文档（如“批准刚才那份设计”“就按 docs/specs/xxx 批准”）；只说“批准了”“可以了”而未指向具体文档的，一律视为模糊。

## 失败模式与回退

| 触发条件 | 一线处理 | 仍失败时 |
|---|---|---|
| 请求本质不是设计 | 明确停止 brainstorming，并路由到实现、review 或 `writing-plans` | 如果用户仍坚持继续，先要求用户指出仍未定的设计问题；如果说不出来，就不要伪造设计流程 |
| 请求同时包含（实现 / 修 bug / 写代码）和（设计 / 想一下 / 比较） | 不编码、不写补丁，只问一个路由确认选择题：`A. 这轮只做设计` / `B. 停止 brainstorming，直接进入实现或调试` | 如果用户没有明确选择，停在路由确认，不继续设计，也不进入实现 |
| 相关上下文找不到，或仓库里没有现成实现可参考 | 明确缺哪些事实，列出 1-3 条工作假设，再只问一个选择题补齐关键约束 | 按最保守假设输出边界级设计，并把风险写进未决事项 |
| 需求跨多个独立子系统 | 先拆成子项目并说明依赖顺序，只继续第一个子项目 | 如果用户要求一次覆盖全部，只写父级设计和拆分清单，不把它伪装成单份可实施设计 |
| 用户回答过宽、同时改变多个前提，或没有真正回答问题 | 把回答收束成单选或多选确认，继续保持一次只问一个问题 | 如果仍然含糊，显式写下当前采用的工作假设，再进入方案比较 |
| 用户要求直接写文档或实施计划，但方案尚未确认 | 先给推荐方案和 trade-offs，然后触发 CHECKPOINT A | 如果用户只要草稿，文档标题显式标注“草稿 / 待审批”，且不要进入 `writing-plans` |
| 用户用含糊话术回应检查点 | 不推进阶段，只要求用户用 `接受：...` 或 `批准：...` 重新确认 | 如果用户拒绝明确确认，停在当前阶段并说明无法安全进入下一步 |
| 问题看起来适合图示，但当前没有可用可视化工具 | 不承诺打开浏览器或生成交互 mockup；改用文本结构、Mermaid 或设计文档里的图示 | 如果图示仍然是关键决策材料，把缺失工具写进未决事项，不伪造已验证视觉方案 |
| 用户跨轮次给出矛盾回答（如前面选方案 A，后面又否定 A） | 显式指出矛盾，引用前面的具体选择，只问一个问题确认以哪一轮为准 | 如果用户确实改主意，回到 PHASE 2-3 重做相关澄清与方案比较，不要假装没矛盾 |
| CHECKPOINT A 通过后，写文档时把方案改得偏离已批准内容 | 把偏离点单独列出，回到用户面前重新触发 CHECKPOINT A 确认方向 | 用户未重新确认前，文档按已批准版本写，偏离写进未决事项 |
| 用户主动拒绝澄清（“你决定吧”“都行”“你来定”） | 点明缺哪个约束最致命，给 1 条最保守假设，只问一个问题确认是否接受 | 用户仍不给 → 按保守假设写边界级设计，假设与风险进未决事项 |

## 默认响应模板

优先用下面的固定形态，减少首轮回复漂移。

### 模板 1：首轮澄清

- 第一段只做一件事：说明先把需求收敛成设计，不进入实现
- 如果请求跨多个独立子系统，第二段先列出子项目，并明确“这轮只继续第一个子项目”
- 最后一段只保留一个问题，默认写成 A / B / C 选择题
- 如果目标、关键约束、成功标准还没钉住，不要在首轮消息里提前展开完整方案比较

默认形态：

> 先把这次讨论收敛成设计文档，不进入实现。
> 这次需求可以拆成 X / Y / Z 三块；这轮我只继续 X。
> 先确认一个点：这次的首要目标更偏 A / B / C？

### 模板 2：方案比较

- 用固定标题 `方案 A`、`方案 B`、`方案 C`
- 每个方案只写 4 项：核心思路、优点、缺点、适用条件
- 最后单独写 `推荐方案`，说明推荐原因和主要 trade-offs
- 收尾只问一个确认问题：接受推荐方案，还是改选 A / B / C

默认形态：

> 方案 A：……
> 方案 B：……
> 方案 C：……
> 推荐方案：……
> 先确认一个点：请回复 `接受：推荐方案`，或改选 `接受：方案 A/B/C`。

### 模板 3：错误路由

- 已经有批准过的设计，或用户明确只要实施计划时，直接停掉 brainstorming
- 路由信息要写清楚“为什么不该继续”以及“下一步要什么输入”

默认形态：

> 这一步不该继续用 brainstorming，应该切到 `writing-plans`。
> 你现在要的是把已定需求拆成实施计划，不需要再做方案比较。

### 模板 4：审批请求

- 文档写完并自检后，直接请求审批，不追加第二个问题
- 只说明文档路径、审批动作和审批后下一步

默认形态：

> 设计文档已写好并保存到 `<path>`。请先审阅这份设计；如果需要改动，我会先改设计，再进入实施计划。
> 如果批准，请回复 `批准：<path>`。

## PHASE 0: 验证请求

开始前先确认：
- 这次请求确实需要设计，而不是单纯实现或审查
- 用户真正想解决的问题、约束和成功标准是什么
- 当前请求是否过大，是否应该先拆成多个子项目

如果需求明显包含多个独立子系统：
- 先指出拆分必要性
- 帮用户明确子项目边界、依赖关系和优先顺序
- 只对第一个子项目继续完整设计流程

## PHASE 1: 探索上下文

先读取最小必要的项目上下文：
- 相关文件、文档、已有接口、命名模式
- 与当前需求直接相关的调用点或测试
- 最近改动中会影响本设计的事实

探索目标不是把项目全部看完，而是回答三个问题：
- 当前系统怎么组织这块能力
- 哪些边界已经存在，应该沿用
- 哪些现实约束会影响设计选择

停止条件：一旦能指出最相关的 owning abstraction、一个邻近调用点或测试、一个会影响方案的现实约束，就停止探索并进入澄清或方案比较；不要为了提高信心继续扩大搜索范围。

## PHASE 2: 一次一个问题地澄清

先阅读 [references/questioning-rules.md](references/questioning-rules.md) 里的规则，再发澄清问题。

澄清阶段的目标：
- 明确目的：为什么要做这件事
- 明确约束：性能、兼容性、时间、依赖、交互方式、发布边界
- 明确成功标准：什么算完成，什么算超范围

规则：
- 每条消息只保留一个待回答的问题
- 如果可以做成选择题，就不要发散成开放题
- 不要在同一条消息里偷偷附带第二个问题
- 首轮澄清默认套用“模板 1：首轮澄清”

如果某个问题属于真实视觉判断，例如布局、界面草图、关系图或架构图，比文字更容易判断，就在那个问题出现时单独询问是否启用可视化辅助。UI 主题本身不等于视觉问题；如果只是需求、范围或 trade-off 选择，继续用文字提问。

## PHASE 3: 提出 2-3 个方案

在你理解需求后，给出 2-3 个方案：
- 每个方案说明核心思路、主要 trade-offs、适用条件
- 优先给推荐方案，并说清推荐原因
- 用 YAGNI 约束设计，不要添加用户没要的复杂度
- 如果现有代码边界已经足够好，优先复用；不要为了“更漂亮”而重做结构
- 输出默认套用“模板 2：方案比较”

方案比较时同时检查设计单元是否足够清楚：每个单元都应该能回答“它负责什么、怎么使用、依赖什么”。如果必须读内部实现才能理解一个单元的用途，或改内部实现会牵动消费者，边界还没有设计好。

## PHASE 4: 分段呈现设计并确认

设计要分段呈现，并在每个重要段落后确认：
- 架构与边界
- 关键组件或模块职责
- 单元接口、使用方式和依赖关系
- 数据流或控制流
- 错误处理与回退策略
- 测试策略

每段内容要和复杂度匹配：
- 简单问题可以是几句话
- 复杂问题可以是更完整的段落

如果用户对某一段提出异议：
- 回到相关问题重新澄清
- 修正设计后再继续下一段
- 每一段收尾只保留一个确认动作，不要顺手开启下一段

🔴 CHECKPOINT A：用户没有用 `接受：...` 或同等明确表述接受推荐方案、范围和关键边界前，不要进入 PHASE 5。

## PHASE 5: 写设计文档

在 CHECKPOINT A 通过（用户用 `接受：推荐方案` 或 `接受：方案 A/B/C` 等明确令牌接受了所选方案）后，把设计写成文档。

路径选择顺序：
1. 用户显式指定路径
2. 仓库已有设计文档约定
3. 默认写到 `docs/specs/<YYYY-MM-DD>-<topic>-design.md`

如需固定骨架，先读取 [references/spec-template.md](references/spec-template.md)。

设计文档至少覆盖：
- 背景与目标
- 范围与非范围
- 方案比较与推荐方案
- 关键边界、组件和职责
- 数据流或控制流
- 错误处理
- 测试策略
- 未决事项

## PHASE 6: Inline 自检

写完文档后，读取 [references/spec-self-review.md](references/spec-self-review.md) 并完成单轮 inline 自检。

修复真正会影响后续计划的问题：
- TODO、TBD、占位符
- 内部矛盾
- 范围过大
- 含糊到会导致写错计划的要求
- 单元边界、接口或依赖关系含糊到实现者必须猜
- 明显过度设计

不要为了文风润色反复循环。修完就继续。

## PHASE 7: 用户审批

自检通过后，明确让用户审阅设计文档。

建议使用类似表述：

> 设计文档已写好并保存到 `<path>`。请先审阅这份设计；如果需要改动，我会先改设计，再进入实施计划。
> 如果批准，请回复 `批准：<path>`。

如果用户要求修改：
- 修改文档
- 重新跑同一轮 inline 自检
- 再次请求审批
- 审批请求默认套用“模板 4：审批请求”

只有在用户明确批准后，才能进入下一阶段。

🔴 CHECKPOINT B：没有 `批准：<path>` 或同等明确批准，就停在设计阶段；不要切到 `writing-plans`。

## PHASE 8: 过渡到 writing-plans

用户批准设计后：
- 明确说明下一步是 `writing-plans`
- 把已批准的设计文档路径作为稳定输入
- 输出下面的 handoff payload，供 `writing-plans` 直接接续：

```text
handoff_to: writing-plans
approved_design: <path>
approval: <用户批准原文>
scope: <本次做什么>
non_scope: <本次不做什么>
open_items: <不阻塞计划的未决事项；没有则写 none>
planning_goal: 基于已批准设计拆成可执行实施计划，不重新做方案比较
```

- 不要在本 skill 中直接转入实现
- 如果用户只批准了方向，没有批准文档，回到 PHASE 5-7，不要跳过审批
- 错误路由（请求本质是实施 / 已有批准设计只要拆计划）已在 PHASE 0 与 🛑 STOP 处理；PHASE 8 默认只做交付，不重复路由

## 反例黑名单

不要这样做：
- 一条消息里塞多个澄清问题，或在推荐后偷偷追加第二个问题
- 目标、约束、成功标准还没钉住，就直接给完整设计或长篇方案比较
- 把跨多个子系统的请求包装成一份巨大设计文档，假装它已经可实施
- 设计未批准就写代码、补丁、测试步骤或实现细节
- 把“看起来可以”“先这样”“继续吧”当成 CHECKPOINT A/B 的明确批准
- 为了“更干净”而重做现有边界、命名或仓库结构
- 没有最小必要上下文时假装已理解系统，而不是显式声明工作假设和未决事项
- 路由到 `writing-plans` 时，重新发起一轮需求澄清或方案比较
- 进入 `writing-plans` 前不提供已批准设计路径、批准原文、范围和非范围
- CHECKPOINT A 通过后、CHECKPOINT B 之前，自行修改已批准方案细节而不重新触发 CHECKPOINT A（silent inter-checkpoint drift）
- 把“非范围”里明确排除的功能偷偷写进设计文档（scope creep via doc）
- 在 PHASE 8 handoff payload 里保留字面量 `<path>` 而不是真实已批准路径

## 关键原则

- One question at a time：一次只问一个问题
- Multiple choice first：优先选择题
- YAGNI：不把未请求功能塞进设计
- Alternatives before commitment：先比较方案，再定设计
- Incremental validation：分段确认，不把整份设计一次性砸给用户
- Visual companion just-in-time：只在真实视觉判断出现且工具可用时启用可视化辅助
- Design for isolation and clarity：每个单元都要职责清楚、接口清楚、依赖清楚
- Follow the repo：优先跟随现有仓库模式和边界

## 立即执行

先验证请求与范围，再读取最小必要上下文，然后按上述流程推进到已批准的设计文档。