schema_version: 1

# romulus 适用性:标 skip/xfail 的 AR + reason + source。
# statuses: applicable(默认) / skip(不调 probe) / xfail(调 probe: 预期 fail->xfail, 意外 pass->xpass)
default: applicable
overrides:
  BMC-8-1-5:
    status: skip
    reason: romulus QEMU 无法仿真 KVM/Virtual Media 硬件 USB gadget 交互, 无 Redfish 只读端点可探测
    source: manual
