# romulus baseline AR — smoke suite(5 AR 可达性门) + mgmt suite(1 skip AR 占位)。
# Schema 布局 v2 (ADR-0025/0027): 薄顶层 + ar_probes.d/<suite>.yaml 分片。
# assert 原语见 tests/baseline/runner/plan.py _PROBE_ASSERTS 兼容矩阵。
# auth: per-interface 凭据; cmd_test_qemu 读 auth.redfish, 缺则 fallback 顶层 user/password。

# ══════════════════════════════════════════════════════════════════
# schema 方言版本 — 布局 v2(薄顶层 + 分片), runner fail-closed 校验
# ══════════════════════════════════════════════════════════════════
schema_version: 2

# ══════════════════════════════════════════════════════════════════
# per-interface 凭据 — redfish/ipmi/ssh_tcp 已实现, console 预留扩展
# ══════════════════════════════════════════════════════════════════
auth:
  redfish:
    user: root
    password: "0penBmc"
  ipmi:
    user: root
    password: "0penBmc"
  ssh:
    user: root
    password: "0penBmc"
  console:
    user: root
    password: "0penBmc"

# ══════════════════════════════════════════════════════════════════
# suite 分片引用 — merge 顺序 = include 顺序 × 分片内原序
# ══════════════════════════════════════════════════════════════════
include:
  - ar_probes.d/smoke.yaml
  - ar_probes.d/mgmt.yaml
