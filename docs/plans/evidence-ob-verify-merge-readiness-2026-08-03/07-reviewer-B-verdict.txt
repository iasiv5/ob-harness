# Blind Reviewer B — verbatim verdict (independent, zero-context, model=opus)
# Spawned: general-purpose subagent, fresh, given ONLY the 4 diffs + 4 prod files +
# ADR-0020 + bestpractice_06 (global exit-code contract) + ob --help. No driver
# conclusions, no cross-reference with Reviewer A. PRIMARY FOCUS: whether smoke's
# exit-1 = α-truth vs the global exit-code contract constitutes a 🔴 blocking tension.
# Verdict rubric: BLOCKER (🔴, should block merge) | NO-BLOCKER.

I have all the evidence I need. All structural checks pass (exit_contract X/Y/Z green, ob_check ALL GREEN, usage_dispatch_sync covers smoke in both dispatch and Commands). I verified the dependencies (`qemu_instance_load` sets `PIDFILE_SSH_PORT/REDFISH_PORT/IPMI_PORT` at qemu_instance.sh:26-42), the bundled change's before-behavior (`confirm_action` at util.sh:428 returns 1 on non-TTY read failure → previously exit 1), and smoke's robustness under `set -euo pipefail`.

---

## Verdicts

**(A) ob — NO-BLOCKER.** Registration (parse_args `smoke)` case at ob:132-137), dispatch (`cmd_smoke` at ob:337-340), and help are coherent. The OVERRIDE banner is present in `ob --help` smoke Options (verified verbatim by running `./ob --help`): "Exit codes (smoke-specific — they OVERRIDE the global '1 = broken' line below)". `exit_contract.py` reports X/Y/Z PASS and `usage_dispatch_sync.sh` asserts dispatch==Commands (smoke is in both). Minor non-blocking notes: (1) the Commands-list shows `smoke [<machine>]` (brackets = optional) while the body says "required positional" and `cmd_smoke` exits 3 with a remedy line if omitted (no interactive fallback, unlike init/build) — a doc nuance, but the exit-3 remedy is clear and recoverable; (2) `parse_args` still accepts `--ssh-port`/`--dry-run`/etc. for smoke and `cmd_smoke` silently ignores them (reads ports only from the PID file) — the help documents "honors NO port overrides ... nor --dry-run", so this is a silent-ignore UX wart, not a contract break.

**(B) lib/qemu_commands.sh — NO-BLOCKER.** `cmd_smoke` (qemu_commands.sh:489-615) is robust under `set -euo pipefail`: every external call is guarded — `if ! qemu_instance_load`/`if ! qemu_instance_is_alive` (the polymorphic 0/1/2 return is consumed, mirroring the comment at :506), `_smoke_wait_ssh_tcp "$s_ssh" || true`, and every probe uses `|| rc=$?` internally with `return 0` always. Ports are read from `PIDFILE_SSH_PORT/REDFISH_PORT/IPMI_PORT` which `qemu_instance_load` populates (qemu_instance.sh:26-42, verified). The IPMI assertion correctly uses `ipmitool -I lanplus` (UDP/RMCP+), not the TCP probe.

**Bundled `cmd_start_qemu` non-TTY change — NO-BLOCKER (sound, non-regressive).** BEFORE (qemu_commands.sh:119-143): non-TTY fresh start called `confirm_action` (util.sh:428), whose `read -r -p` returns non-zero on EOF/closed stdin → `confirm_action` returns 1 → `case *) exit 1`; so non-TTY `ob start-qemu <machine>` (fresh, no instance) either exited 1 or required piped "y". AFTER: `if [[ -t 0 ]]` gates the confirmation + 3s countdown to TTY only; non-TTY proceeds with an info line. Critically, the PATH-RISKY operation (killing an existing instance) is in the conflict block (qemu_commands.sh:77-101), which STILL requires `--force` in non-TTY (`else ... exit 1` at :97-100) — unchanged. TTY users retain confirmation + countdown. `[[ -t 0 ]]` is the same stdin-TTY check used by cmd_stop_qemu (:252) and the existing conflict block (:82), so it is consistent. The bundling is scope-adjacent but directly serves the branch's stated `start-qemu → smoke → stop-qemu` E2E CI story (ADR-0020), is documented in-comment, and introduces no regression for interactive users. Not a blocker.

**(C) lib/smoke_assertions.sh — NO-BLOCKER.** Five leaf-pure judges (smoke_assertions.sh:18-105), each `return 0`/`return 1` and echoes one ✓/✗ line — no `exit` anywhere. Registered in `LEAF_EXIT_EXCEPTIONS_BY_BASENAME` with empty exception set; `exit_contract.py` Y PASS confirms no judge exits. Judgments use structural markers (not pure HTTP 200): `@Redfish.Copyright`/`RedfishVersion`/`ServiceRoot.v1` for root; `ManagerType`/`"UUID"`/`#Manager` for Managers — preventing placeholder/placeholder-page false-pass. The swversion regex `(SoftwareVersion|FirmwareVersion)"[[:space:]]*:[[:space:]]*"([^"]+)"` correctly tolerates JSON whitespace and the `[^"]+` ≥1 guard rejects empty-string. The gb200nvl RMCP+ gap is documented in-code (smoke_assertions.sh:35-40) and ADR-0020 as legitimate α ✗, not a defect.

**(D) tools/exit_contract.py — NO-BLOCKER.** Single line (exit_contract.py:78) adding `'smoke_assertions.sh': set()` to `LEAF_EXIT_EXCEPTIONS_BY_BASENAME`. This STRENGTHENS the Y check: without this key, `check_Y` would never inspect smoke_assertions.sh (it iterates the dict's basenames); with it, Y enforces that no judge ever calls `exit` (verified PASS). Correct and beneficial.

**(CROSS) PRIMARY FOCUS — NO-BLOCKER.** The exit-1 = α-truth vs the global "1 = broken" contract is a genuine semantic tension, but it is resolved by THREE documented layers and does not meet the HIGH bar for 🔴: (1) the per-command OVERRIDE banner in `ob --help` smoke Options — "Exit codes (smoke-specific — they OVERRIDE the global '1 = broken' line below)" — verified verbatim; (2) the in-band stderr line emitted AT exit-1 time (qemu_commands.sh:613): `warn "exit 1 here is the α truth-reporter contract: ... NOT a smoke command failure" >&2` — deliberately routed to stderr so it doesn't pollute the stdout truth report; (3) ADR-0020 (Status: accepted) documents the trade-off and its consequences including the known gb200nvl RMCP+ gap. Crucially, bestpractice_06's own step 4 instructs the agent to "先读 ob 自己的报错 + 相关 ob 源码定位根因" BEFORE falling back — an agent following this reads the α disclaimer + the ✗ rows + RAW responses and correctly understands the BMC interface (not smoke) is the subject. The realistic misuse risk is bounded to an agent that keys SOLELY on exit code and ignores ob's output — but that is a pre-existing failure mode for EVERY ob subcommand (such an agent would also miss exit-3 remedy lines and exit-2 cancel messages), not something smoke introduces. The trade-off is sound: α yields zero per-machine knowledge and a deeper module (5 assertions); the rejected β (`--allow-fail`/expected-profile) was rejected precisely because it would couple smoke to per-machine image-profile data (ADR-0020 option 3). Non-blocking concerns I'll NOTE: (a) the global "Exit Codes" section appears BELOW the smoke section in `--help`, so a hasty reader could anchor on the global "1 = Failure — broken" line without scrolling up to smoke's override — a cross-reference from the global table to "see smoke Options for an exception" would harden it; (b) the resolution leans on the agent actually reading smoke-specific output, which is bestpractice_06-compliant but not zero-risk. These are 🟡 hardening opportunities, not 🔴 blockers — the contract violation is documented, deliberated, and three-times mitigated.

---

**SUMMARY: blockers=0 no-blockers=5**
