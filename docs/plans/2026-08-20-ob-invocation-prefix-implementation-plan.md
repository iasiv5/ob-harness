# ob invocation prefix 统一实施计划（OB_CMD 变量 + ./ob 文案 + tests/docs 同步）

## 目标

消除「提示用户/agent 敲 `ob ...` 但 ob 未装 PATH → `ob：未找到命令`」的问题。做法：引入 invocation prefix 变量 `OB_CMD`（值 = 用户敲法 `$0`，如 `./ob`），所有 user-facing「下一步命令」文案（remedy line、Tip、Next step、连接 summary、usage）引用 `$OB_CMD` 而非 hardcode `ob`；tests 断言与 agent 指令文档同步为 `./ob`。

设计共识来自 2026-08-20 /grill-with-docs 会话（本仓未立 ADR——纯文案/排版变更，不满足 surprising 门槛）。

## 架构快照

- `ob` 入口在 source `lib/*.sh` 前已有 `OB_ENTRY_DIR` 赋值点（ob:72 附近），`OB_CMD` 落同处。
- `lib/util.sh` 顶部（log 函数族前）放兜底 `OB_CMD="${OB_CMD:-ob}"`：tests 经 `tests/lib/ob_loader.sh` 直接 source lib 调函数、不经 ob 入口，无兜底会 nounset 炸。
- `tests/lib/ob_loader.sh` 已算出 `OB="$OB_DIR/ob"`（第 8 行），在其后 **`export`** `OB_CMD="${OB_CMD:-./ob}"`——必须 export：20 个测试文件存在二级 `bash -c 'OB_NO_MAIN=1 source "$1"; ...' _ "$OB"` child 模式（如 `tests/protocol/qemu_launch_profile_remedy.sh:37`），child bash 不继承未 export 的 shell 变量，且 child 里 `$0` 是 `_`，不 export 会渲染出 `OB_CMD=_`。
- **`$0` 回显的已知局限**：`bash ob` 形态下脚本内 `$0` 只是 `ob`（不是 `bash ob`），回显裸 `ob` 照敲会 command not found。本次只承诺 `$0` 可见的调用形态（`./ob`、绝对路径），`bash ob` 为已知局限不处理（重建 interpreter 前缀超出纯文案范围）。
- usage heredoc（`ob` 的 `usage()` 与 `lib/qemu_commands.sh` 的 `test_qemu_usage()`）均为 **unquoted `<<EOF`**，`$OB_CMD` 直接展开；已核实两份 heredoc 文本无其它 `$` 变量会被误展开。EXIT_RE（`tools/exit_contract.py`）不解析 heredoc，不受影响。
- 仓库无内部 `ob` 自调用（已 grep 核实），无需处理自调用路径。

## 全局约束

- **exempt 清单（不改）**：
  - 生成文件注释的身份标识：`lib/repo.sh:161`、`lib/init_pipeline.sh:384/385/388/439/448`（`Auto-generated by ob init ...` 等）、`lib/qemu_binary.sh:36`（`auto-managed by 'ob start-qemu'` 配置头注释）。
  - 错误标签/指称（非可敲命令）：`"ob build: failed to ..."`、`"ob dev $subcmd: ..."`、`devtool_search.sh` 全部 `ob dev refresh:` stderr 前缀、`devtool_intake.sh:61` 菜单标题 `ob dev subcommands:`。
  - `README.md`（已通篇 `./ob`）、`rules/knowhow/workflow_01-obmc_env_init.md`（已 `./ob`）、全部 ADR 与 `docs/plans/` 历史文档、`tests/baseline/README.md`（谱系指称）。
- **文案规则**：引号内包含完整可敲命令（`Run './ob init X' first.`），不拆成 `'./ob' init X`；不借机统一包裹风格（只换前缀）。
- **EXIT_RE 契约**：usage heredoc 散文避免 "exit code"（exit 后空格），用 "exit-code" 连字符（既有约束，本次新增文案不触碰 exit 文案即可）。
- 单一 commit 交付（lib+ob+tests+docs 原子合入）；计划分 task 不分 commit。

## 文件结构与职责

| 文件 | 改动职责 |
|---|---|
| `ob` | 入口在 `OB_ENTRY_DIR` 后按 `OB_NO_MAIN` 分流设置 `OB_CMD`：source/test 模式 `OB_CMD="${OB_CMD:-$0}"`（外部注入优先），普通 CLI 强制 `OB_CMD="$0"`（防环境污染）；`usage()` heredoc 的 Usage 行/Examples/smoke·test-qemu 段 `ob xxx` → `$OB_CMD xxx`；加一行「不装 PATH，可在 repo root 用 `./ob` 或用脚本路径调用」的说明 |
| `lib/util.sh` | 顶部兜底 `OB_CMD="${OB_CMD:-ob}"`；`resolve_npm_registry` 失败提示里两处 `ob build` → `$OB_CMD build`（util.sh:367/371） |
| `lib/qemu_commands.sh` | remedy/Tip/info 约 17 处 + `test_qemu_usage()` heredoc 2 处 → `$OB_CMD` |
| `lib/commands.sh` | require_path remedy 2 处、nontty remedy 3 处、build 恢复提示 1 处、Tip 行 411（已是 `./ob` hardcode，迁 `$OB_CMD`） |
| `lib/qemu.sh` | 连接 summary 2 处（261/293） |
| `lib/qemu_binary.sh:500` | remedy 1 处 |
| `lib/qemu_launch_profile.sh` | require_path remedy 2 处（245/246）+ 192/270 remedy |
| `lib/machine_resolve.sh` | remedy 2 处（35/44） |
| `lib/repo.sh` | remedy 3 处（177/333 + 274/310 指令性散文） |
| `lib/init_pipeline.sh` | remedy 2 处（106/240）；384-448 注释 exempt |
| `lib/init_intake.sh:43` | nontty remedy 1 处 |
| `lib/status_render.sh` | Next step 1 处（86）+ tips 3 处（94-96） |
| `lib/bare_mirror.sh:283` | retry 提示 1 处 |
| `lib/devtool_subcmd.sh` | remedy 5 处（32/33/97/162/184）；标签 exempt |
| `lib/devtool_intake.sh` | remedy 2 处（100/113） |
| `tests/lib/ob_loader.sh` | `export OB_CMD="${OB_CMD:-./ob}"`（export 保证二级 child bash 继承） |
| tests 断言（17 文件 + golden） | `Run 'ob ...'` / `: ob xxx` 字面量 → `./ob`（详见 Task 6 清单，含 `tests/orchestration/qemu_launch_profile.sh` 与 `tests/protocol/status_golden.expected`） |
| `AGENTS.md`、`rules/knowhow/bestpractice_06-ob_first.md` | 祈使句命令 `ob --help`/`ob <cmd>` → `./ob`（指称保留） |
| `CONTEXT.md` | 词条内 remedy 示例字面量 `Run 'ob ...' first` → `./ob`（remedy line 词条已于计划前同步；剩 179/195/199 行的示例） |

## 任务清单

### Task 1: OB_CMD 变量三处落位

**Consumes**: 无 **Produces**: 全局 `OB_CMD`——ob 入口跑时 = `$0`（用户敲法），lib 直 source 时 = `ob`（util.sh 兜底），test loader 环境下 = `./ob`

Run:
```bash
# 1) lib/util.sh 顶部（log() 定义前）加：
#    OB_CMD="${OB_CMD:-ob}"   # invocation prefix: 用户敲法($0)由 ob 入口覆盖; tests 直 source lib 时兜底
# 2) ob 入口 OB_ENTRY_DIR= 行（ob:72 附近）后加（不复用 :- 兜底: 环境已有 OB_CMD=ob 的普通 CLI 会被污染成回显 bare ob，绕回本次问题）：
#    if [[ -n "${OB_NO_MAIN:-}" ]]; then
#        OB_CMD="${OB_CMD:-$0}"   # test source 模式(ob_loader export 的 ./ob 优先, 兜底 $0)
#    else
#        OB_CMD="$0"              # 普通 CLI 强制以 $0 为准($0 可见形态: ./ob / 绝对路径; bash ob 形态只回显 ob, 已知局限)
#    fi
#    须在 source lib 前(util.sh 的 :- 兜底不覆盖已有值)
# 3) tests/lib/ob_loader.sh 的 OB= 行（第 8 行）后加：
#    export OB_CMD="${OB_CMD:-./ob}"   # 必须 export: 二级 bash -c child source "$OB" 不继承非 export 变量, 且 child 里 $0=_
```
Expected/验证（child 传播是本设计最快证伪点，最先跑）:
```bash
bash -c 'source tests/lib/ob_loader.sh; bash -c "OB_NO_MAIN=1 source \"\$1\"; echo \"[\$OB_CMD]\"" _ "$OB"' | grep -xF '[./ob]'
bash -c 'source lib/util.sh && echo "[$OB_CMD]"' | grep -F '[ob]'
OB_CMD=ob bash -c 'OB_NO_MAIN=1 source "$1"; echo "[$OB_CMD]"' _ "$PWD/ob" | grep -xF '[ob]'   # source 模式: 外部注入优先, 不被 $0 覆盖
OB_CMD=ob ./ob --help | head -1 | grep -F 'Usage: ./ob [command]'   # 普通 CLI: 受污染环境下仍强制 $0(单次调用, 勿拆两次丢掉污染环境; Task 2 后)
```
前两条先行；后两条分别证明 source 模式注入优先、CLI 模式强制 $0（第 4 条并入 Task 2 后跑）。

### Task 2: ob usage() heredoc 改 $OB_CMD + repo-root 说明

**Consumes**: Task 1 的 `OB_CMD` **Produces**: `./ob --help` 输出随 `$0` 可见调用形态回显

改动点：`Usage: ob [command]...` → `Usage: $OB_CMD [command]...`；Examples 段全部 `ob xxx` 行（init/build/start-qemu/stop-qemu/deploy-to-qemu/smoke/dev，约 15 行）；smoke Options 段 2 处、test-qemu 段 1 处 `'ob start-qemu <machine>'`；Exit Codes 段 `run the suggested 'ob ...'`。另在 `Run without arguments` 行后加一行：`ob is not installed to PATH; run ./ob from the repo root or invoke this script by path. Hints below echo how you invoked it.`——不写 "must be run from the repo root"（harness root 由 `OB_ENTRY_DIR` 定位、与 cwd 无关，绝对路径/`../ob-harness/ob` 调用都是支持形态，别误说成不支持）；该行不含 "exit code"（EXIT_RE）。

验证:
```bash
./ob --help | head -1 | grep -F 'Usage: ./ob [command]'
! ./ob --help | grep -nE "'ob [a-z-]|run the suggested 'ob|^[[:space:]]+ob [a-z-]"   # 四类残留全清零: Usage 行首 / Examples 行首 / smoke·test-qemu 段引号内命令 / Exit Codes 段 suggested 'ob'
```

### Task 3: test_qemu_usage() heredoc（lib/qemu_commands.sh:856 起）

改动点：`Usage: ob test-qemu <machine> [options]` 与段内 `'ob start-qemu <machine>' first.` 两处。

验证:
```bash
bash -c 'source tests/lib/ob_loader.sh; test_qemu_usage 2>/dev/null | grep -cF "./ob "' | grep -qx 2
```

### Task 4: QEMU 族 lib 文案换 $OB_CMD

文件：`lib/qemu_commands.sh`（57/60/64/86/97/137/220/221/348/477/677/683/953 附近 nontty/无实例 remedy、664/963 specify-machine、1015/1018/1031 状态指认、1095/1101）、`lib/qemu.sh`（261/293）、`lib/qemu_binary.sh`（500）、`lib/qemu_launch_profile.sh`（192/245/246/270）。
注意：953 行即用户报告的那条 `run 'ob start-qemu <machine>' first.`。1015/1018/1031 中 `via 'ob status'` / `re-run ob init` 是指令性 → 改；`tests/baseline/README.md` 指称不动。

验证:
```bash
! grep -n "Run 'ob \|: ob [a-z-]*\b\|recover manually: ob \|re-run 'ob \|re-run ob init\|'ob status'\|'ob build'\|'ob stop-qemu \$MACHINE'\|    ob \(start\|stop\)-qemu" lib/qemu_commands.sh lib/qemu.sh lib/qemu_binary.sh lib/qemu_launch_profile.sh
```
（grep 无输出 = 0 命中；`ob dev refresh:`/标签类 exempt 不匹配上述模式）

### Task 5: 其余 lib 文案换 $OB_CMD

文件与行（见文件结构表）：`commands.sh`（109/111/119/356 的 remedy 与 specify-machine、218、411）、`util.sh`（367/371）、`machine_resolve.sh`、`repo.sh`、`init_pipeline.sh`（106/240）、`init_intake.sh`（43）、`status_render.sh`（86/94/95/96）、`bare_mirror.sh`（283）、`devtool_subcmd.sh`（32/33/97/162/184）、`devtool_intake.sh`（100/113）。
exempt 再确认：`commands.sh:124` 等标签行、`devtool_intake.sh:61` 菜单标题、`devtool_search.sh` 全部、`init_pipeline.sh:384-448` 生成注释。

验证:
```bash
! grep -n "Run 'ob \|Run \"ob \|: ob [a-z]\|Re-run: ob \|Next step : ob \|retry: ob \|Tip: CLI mode — ./ob" lib/commands.sh lib/util.sh lib/machine_resolve.sh lib/repo.sh lib/init_pipeline.sh lib/init_intake.sh lib/status_render.sh lib/bare_mirror.sh lib/devtool_subcmd.sh lib/devtool_intake.sh
```
另确认替换确实引用变量而非新 hardcode（逐文件判 fail，不靠多文件 grep 的整体 rc）：
```bash
fail=0; for f in lib/commands.sh lib/util.sh lib/machine_resolve.sh lib/repo.sh lib/init_pipeline.sh lib/init_intake.sh lib/status_render.sh lib/bare_mirror.sh lib/devtool_subcmd.sh lib/devtool_intake.sh; do grep -q '\$OB_CMD' "$f" || { echo "missing OB_CMD: $f"; fail=1; }; done; exit "$fail"
```
退出码 0 = 每文件 ≥1。

### Task 6: tests 断言字面量同步（17 文件 + 1 golden）

文件与命中数（`ob ` → `./ob `，仅限 remedy/specify/Tip 断言与 grep 模式，标签断言不动；命中数为计划时 grep 计数，以实际 grep `Run 'ob \|: ob \|Next step : ob ` 为准逐处判断改或不改）：
- `tests/protocol/`：`build_noninteractive.sh`(4)、`deploy_to_qemu_machine_selection.sh`(2)、`qemu_launch_profile_remedy.sh`(4 断言 + **第 45 行 grep 模式 `Run 'ob ` 同步为 `Run './ob `**，否则 remedy 计数失配)、`smoke_exit_contract.sh`(1)、`smoke_ob.sh`(1)、`smoke_substep_isolation.sh`(4)、`smoke_surface.sh`(4)、`start_qemu_remedy.sh`(7)、`status_machine_state.sh`(3)、`test_qemu_surface.sh`(1)、`usage_dispatch_sync.sh`(1)、`status_golden.expected`（33-35 行的 `Next step : ob init orphan` 与 `Run 'ob build <machine>'` golden 同步——status_render 改后 golden 必须同步否则 diff 红）
- `tests/orchestration/qemu_launch_profile.sh`（182 行 `Run 'ob build ast2700a1-evb' first.`——run_all 默认跑 orchestration 层，漏改 ob_check 必红）
- `tests/unit/`：`devtool_dispatch.sh`(19)、`devtool_subcmd.sh`(8)、`machine_resolve.sh`(2)、`status_render.sh`(2)
- **不改**：`tests/fixtures/smoke_help_cases.sh`（模拟用户提问的输入语料，非被测文案输出）

验证（Task 4/5 之后应全绿；若有失败按实际输出修断言或定位漏改点，不许反向改文案迁就断言）:
```bash
fail=0
for t in build_noninteractive deploy_to_qemu_machine_selection qemu_launch_profile_remedy smoke_exit_contract smoke_ob smoke_substep_isolation smoke_surface start_qemu_remedy status_machine_state test_qemu_surface usage_dispatch_sync status_golden; do bash "tests/protocol/$t.sh" || { echo "FAIL: protocol/$t"; fail=1; }; done
bash tests/orchestration/qemu_launch_profile.sh || { echo "FAIL: orchestration/qemu_launch_profile"; fail=1; }
for t in devtool_dispatch devtool_subcmd machine_resolve status_render; do bash "tests/unit/$t.sh" || { echo "FAIL: unit/$t"; fail=1; }; done
exit "$fail"
```
退出码 0 且无 `FAIL:` 行为通过信号。

### Task 7: agent 指令文档 + CONTEXT.md 示例同步

- `AGENTS.md:41`：`先 \`ob --help\` 查` → `先 \`./ob --help\` 查`；`就走 \`ob <cmd>\`` → `就走 \`./ob <cmd>\``；「`ob --help` 是唯一权威能力清单」同理。
- `rules/knowhow/bestpractice_06-ob_first.md`：祈使句的 `ob --help`（13/17/19/21/23/24 行）与 48 行 remedy 链 `ob init <machine>` → `ob build` → `ob start-qemu` 改 `./ob`；命令名列表（init/build/status/...）与指称（"`ob` 为唯一前门"）保留。
- `CONTEXT.md`：179/195/199 行词条内 `Run 'ob ...' first` 示例字面量 → `./ob`；命令指称（`ob smoke` 等）与 `_Avoid_` 行不动。
- 判据（写进 commit message 亦可）：该行是否被期望原样敲进 shell。

验证:
```bash
fail=0
if grep -n 'ob --help\|ob <cmd>' AGENTS.md | grep -v '\./ob'; then echo "unexpected: AGENTS.md 指令残留"; fail=1; fi
if grep -n 'ob --help\|ob <cmd>' rules/knowhow/bestpractice_06-ob_first.md | grep -v '\./ob'; then echo "unexpected: bp_06 指令残留"; fail=1; fi
if grep -n "Run 'ob " CONTEXT.md; then echo "unexpected: CONTEXT.md 示例残留"; fail=1; fi
grep -n "\./ob init <machine>.*\./ob build <machine>.*\./ob start-qemu" rules/knowhow/bestpractice_06-ob_first.md || fail=1   # 断言带 ./ 前缀: 'ob init' 是 './ob init' 的子串, 不带 ./ 的正则区分不了已修/没修
exit "$fail"
```
前两条命中输出时逐行人工确认是「命令名列表 / 以 `ob` 为唯一前门」类指称（改 fail=0）还是漏改的指令（保持 fail=1）；第四条命中的 remedy 链应已为 `./ob init ... → ./ob build ... → ./ob start-qemu ...`。退出码 0 为通过。

### Task 8: 最终验证 + 单 commit

```bash
bash tools/ob_check.sh; rc=$?; echo "ob_check rc=$rc"; exit "$rc"
# 单独 shell 里另跑: git status --short   确认改动面与文件结构表一致，无意外文件
```
预期：ob_check 全绿（结构/函数登记/shellcheck baseline/测试）；run_all 无 FAIL；然后单 commit（`feat(ob): unify invocation prefix via OB_CMD — user-facing hints echo $0` 风格，中文正文按仓库惯例），一个 commit 收口全部文件。

## 执行纪律

- 开始实现前，先批判性复查整份计划；发现缺项、矛盾、命名不一致或验证命令无效，先修计划。
- 按任务顺序执行，不无声跳步、合并步或改变任务目标。
- 每完成一个任务，运行该任务定义的验证。
- 遇到阻塞、重复失败或计划与仓库现实不符（如行号漂移——以模式 grep 定位为准，行号只是初始锚点），立即停下说明，不要猜。
- 当前在 main 分支：开始实现前与用户确认（或按用户指示切分支）。
- 全部任务完成后，运行最终验证并输出修改摘要。

## 最终验证

Task 8 的三条命令 + 向用户演示原始场景收敛：`./ob test-qemu`（无实例时）应输出 `run './ob start-qemu <machine>' first.`，照抄执行不再 command not found。
