# Reviewer B (synchronous, opus, zero-shared-context) — independent verdicts
# Re-ran all cited greps/reads before deciding. Read .github/workflows/ob-tests.yml + coverage_radar.py L86-89 to hard-confirm F4 + F5 mechanism.
F1: KEEP — grep -rln download_and_replace_community_qemu tests/ returns empty and the orchestration test calls _dlqbc_stage_binary/_replace_community_binary directly in subshells (tests/orchestration/qemu_binary_replace.sh:28,38,55,70), never the wrapper whose 3 branches (acquire-fail L222-226, flock-busy L230-235, commit L238-243) are therefore unexercised, yet matrix L81 lists the wrapper in the 涉及函数 column.
F2: KEEP — The only tests/ reference is a negative structural assertion (qemu_launch_profile_structure.sh:78), no .exp covers it (grep of tests/integration/ and all .exp files empty), so the wrapper's non-TTY-exit-3 / prompt-cancel / resolve-error-loop branches are genuinely untested behaviorally — and matrix L82's "非 TTY exit 3 仍靠 .exp" boundary note points at coverage that does not in fact exist.
F3: KEEP — Both files assert the identical contract via cmd_build, smoke_ob.sh:38's cwd-based path subsumes exit_codes.sh:84's override-based path, the file is the slowest at 19.54s, and the finding correctly preserves the 4 non-TTY-guard + positional cases smoke_ob.sh does not cover.
F4: KEEP — ob_check.sh does invoke run_all.sh (L213-217), so within every canonical bash tools/ob_check.sh run the extract_funcs/baseline/exit-contract segments execute twice (once at ob_check.sh step 1, again via run_all → ob_check_smoke.sh → ob_check.sh SKIP_TESTS), making this 5.07s / 8.8% test redundant for the canonical workflow; option (b) "keep but document" is a sound no-cost remedy.
F5: DROP — The cross-check output [05:123] already self-explains these 5 names as "应为 surface gate 等刻意 out-of-radar" and matrix L113 annotates them "out-of-radar(surface gate 回归锁...cross-check out-of-scope 列)", so the "recurring false-positive noise" is 5 lines of self-documenting output rather than a measurable over-engineering cost, the entry is legitimate annotated documentation of an intentional gate (not a stale coverage record), and the proposed allowlist/move remedies a non-issue at non-zero maintenance cost.
SUMMARY: kept=4 dropped=1
