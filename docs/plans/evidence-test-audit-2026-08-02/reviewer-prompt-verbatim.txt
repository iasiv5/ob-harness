# Verbatim reviewer prompt — used IDENTICALLY for both independent reviewers
# (spawned as fresh general-purpose subagents, model=opus, run_in_background=false,
#  zero-shared-context: separate spawns, no transcript sharing, no cross-reference).
# Any fresh agent can re-run: spawn 2 subagents with this exact prompt + the evidence
# pack (files 01-10 in this directory) and reproduce the consensus.

===PROMPT BEGIN===
You are an independent test-framework audit reviewer. For each of 5 findings below,
return a KEEP or DROP verdict with a one-sentence reason grounded ONLY in objective
evidence. You are one of >=2 independent reviewers; you must NOT assume any other
reviewer exists or what they might say. Decide solely from the evidence.

## Repository context (read-only)
cwd: /home/iasi/ob-harness (git repo, branch feat/ob-verify). Audit target = test
framework: tests/* and tools/<gate>. Production code (ob, lib/*.sh) is the test
TARGET only.

Evidence-pack raw captures in docs/plans/evidence-test-audit-2026-08-02/:
- 01-enumeration-tests.txt, 02-enumeration-tools-gates.txt
- 03-coverage-radar.txt (structure-only, 231 fn all uncovered)
- 04-coverage-matrix.txt
- 05-coverage-crosscheck.txt (THE real coverage picture)
- 06-radar-uncovered-grep.txt (grep tests/ for 3 radar-uncovered functions)
- 07-runall-timing.txt (fast run_all = 57.7s; per-layer breakdown)
- 08-slowest-tests.txt (slowest protocol .sh)
- 09-overlap-exit-build.txt (exit_codes.sh x smoke_ob.sh overlap + matrix stale-ref)
- 10-canon-enumeration.txt (canonical find tests -type f = 117 + 10 tools gates)

You may (and should) re-run any cited grep/command yourself to verify. Do NOT edit
any file. Do NOT spawn sub-agents.

## The 5 findings
[F1-F5 with full 5-field detail + falsifiable evidence — see plan doc finding table.
 Each finding states: type | file:line | status quo | proposed change | risk |
 cited evidence (grep command + evidence-pack file). Findings F1-F5 verbatim from
 docs/plans/2026-08-02-test-framework-optimization-plan.md "Findings" section.]

## Rubric (apply independently per finding)
Return one line per finding:
  F<n>: KEEP|DROP — <reason grounded in objective evidence>

- KEEP if the finding identifies a REAL evidence-backed over-eng cost OR a real
  cov-gap in a production-behavior contract, AND the proposed change is sound +
  proportionate.
- DROP if the evidence doesn't support it (e.g. "duplicate" is actually a
  meaningfully different assertion; "untested" function is in fact exercised;
  cost not measurable as stated), OR proposed change is out-of-scope / higher-risk
  than warranted, OR finding is purely subjective.
- Base decision ONLY on the evidence above + captured .txt files + your own re-runs.
  Do not invent evidence.

End with: SUMMARY: kept=<n> dropped=<n> (tally of your own 5 verdicts).

Be rigorous and skeptical. Verify don't trust.
===PROMPT END===

# The full per-finding text fed to each reviewer is the "Per-finding detail" section
# of docs/plans/2026-08-02-test-framework-optimization-plan.md (F1-F5, including all
# cited evidence commands + evidence-pack file references). Reproduce verbatim from
# that section to re-run the review.
