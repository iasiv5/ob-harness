# ob init 自动并行度调优 + managed block 迁移 实施计划

## 目标

将已批准的设计文档 `docs/specs/2026-06-07-ob-init-auto-parallelism-design.md` 落地为 `ob` 脚本代码改动。

完成标志：
- WSL 环境下 `ob init` 自动在 `.inc` 中写入 `BB_NUMBER_THREADS` / `PARALLEL_MAKE`，值符合资源公式
- 非原生 Linux 环境不受影响
- `local.conf` 不再包含 `# BEGIN/END ob init managed settings` block
- `CONNECTIVITY_CHECK_URIS` 从 managed block 迁入 `.inc`
- 用户在 `local.conf` 中用 `=` 覆盖 `.inc` 中的 `??=` 变量时，覆盖生效

## 输入工件

- 设计文档：`docs/specs/2026-06-07-ob-init-auto-parallelism-design.md`

## 架构快照

`.inc` 成为 `ob init` 管理的唯一配置文件（ob 全权管理，每次覆盖重写）。`local.conf` 回归用户领地，`ob init` 只负责添加 `include` 行。

`ensure_bootstrap_local_conf()` 职责收缩为：GITLAB_IP 检测 + include 行添加。managed block 逻辑整体删除。

`generate_build_config()` 职责扩展为：WSL 检测 + 资源公式计算 + 生成完整 `.inc`（含 INHERIT、CONNECTIVITY_CHECK_URIS、DL_DIR、SSTATE_DIR、并行度）。

## 文件结构与职责

- Modify: `ob`
  - `ensure_bootstrap_local_conf()`（`ob:397-569`）：删除 managed block 逻辑，保留 GITLAB_IP 检测和 include 行添加
  - `generate_build_config()`（`ob:1332-1425`）：新增 WSL 检测 + 资源公式 + 扩展 `.inc` 生成内容
  - 新增两个辅助函数：`detect_wsl()`、`calc_parallelism()`

## 任务清单

### Task 1: 新增 WSL 检测和资源计算辅助函数

- 目标：在 `ob` 脚本中新增 `detect_wsl()` 和 `calc_parallelism()` 两个辅助函数
- 涉及文件：`ob`（在 `detect_harness_root()` 函数之后、`list_available_machines()` 之前插入）
- 验证范围：函数可以被后续 Task 2 调用

- [ ] Step 1: 确认插入位置
  - Run: `grep -n "detect_harness_root\|list_available_machines" /home/iasi/ob-harness/ob | head -4`
  - Expected: `detect_harness_root()` 在 785 行附近，`list_available_machines()` 在 801 行附近，两个函数之间有空间插入

- [ ] Step 2: 写最小实现
  - Change: 在 `detect_harness_root()` 闭合 `}` 之后、`list_available_machines()` 之前，插入两个函数：

```bash
detect_wsl() {
    grep -qi microsoft /proc/version 2>/dev/null
}

calc_parallelism() {
    local mem_total_kb swap_total_kb mem_gb swap_gb total_gb cores n
    mem_total_kb=$(grep -oP '^MemTotal:\s+\K\d+' /proc/meminfo 2>/dev/null || echo 0)
    swap_total_kb=$(grep -oP '^SwapTotal:\s+\K\d+' /proc/meminfo 2>/dev/null || echo 0)
    mem_gb=$((mem_total_kb / 1048576))
    swap_gb=$((swap_total_kb / 1048576))
    total_gb=$((mem_gb + swap_gb))
    cores=$(nproc 2>/dev/null || echo 1)
    # Each gcc process budgeted ~4 GB; cap at physical core count; minimum 1
    local budget=$((total_gb / 4))
    if [[ "$budget" -lt 1 ]]; then budget=1; fi
    if [[ "$budget" -gt "$cores" ]]; then budget=$cores; fi
    echo "$budget"
}
```

- [ ] Step 3: 验证函数语法
  - Run: `bash -n /home/iasi/ob-harness/ob && echo "syntax OK"`
  - Expected: `syntax OK`

---

### Task 2: 改造 generate_build_config() 生成完整 .inc

- 目标：让 `generate_build_config()` 在 `.inc` 中写入完整配置：INHERIT、CONNECTIVITY_CHECK_URIS、DL_DIR/SSTATE_DIR（??=）、WSL 并行度（??=）
- 涉及文件：`ob`（`generate_build_config()` 函数体，`ob:1332-1425`）
- 验证范围：WSL 环境下 `ob init` 后 `.inc` 包含所有预期变量

- [ ] Step 1: 确认当前 .inc 生成逻辑
  - Run: `grep -n "echo.*DL_DIR\|echo.*SSTATE_DIR\|echo.*INHERIT\|}> \"\$inc_file\"" /home/iasi/ob-harness/ob | head -10`
  - Expected: 定位到写入 `.inc` 的 `{ ... } > "$inc_file"` 代码块（约 1377-1393 行）

- [ ] Step 2: 替换 .inc 生成逻辑
  - Change: 将 `generate_build_config()` 中从 `# Check if DL_DIR...` 注释开始直到函数末尾的所有代码（约 1356-1425 行）替换为以下内容。这包括：删除 `has_dl_dir`/`has_sstate_dir` 变量、删除条件 DL_DIR/SSTATE_DIR 写入、删除条件 mkdir、删除旧的 info 输出、删除 include 行添加逻辑（已移至 Task 3 的 `ensure_bootstrap_local_conf`）。只保留 timestamp、backup、WSL 检测和新的 `.inc` 生成块：

```bash
    # WSL parallelism detection
    local is_wsl=0
    local parallel_n=""
    if detect_wsl; then
        is_wsl=1
        parallel_n=$(calc_parallelism)
        info "WSL detected — parallelism limited to -j${parallel_n} (memory budget)"
    fi

    local timestamp
    timestamp=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

    # Backup existing .inc file (keep only the most recent backup)
    if [[ -f "$inc_file" ]]; then
        rm -f "${inc_file}".bak.* 2>/dev/null || true
        cp "$inc_file" "${inc_file}.bak.$(date +%Y%m%d%H%M%S)"
        info "Previous .inc backed up (old backups cleaned up)"
    fi

    # Generate .inc file — ob init managed configuration.
    # All variables use ??= (weak default) unless a stronger operator is required.
    # User can override any ??= variable in local.conf with a plain = assignment.
    {
        echo "# Auto-generated by ob init $MACHINE at $timestamp"
        echo "# Do not edit manually. Re-run 'ob init $MACHINE' to regenerate."
        echo ""
        echo "# Enable externalsrc class (no-op when no EXTERNALSRC_pn-* is set)."
        echo "# Use 'ob dev <recipe>' to add externalsrc entries for specific recipes."
        echo 'INHERIT += "externalsrc"'
        echo ""

        echo "# Disable OE connectivity probes by default; github fetches already validate network reachability."
        echo '# Note: uses = (not ??=) because OE-core sets CONNECTIVITY_CHECK_URIS ?= in default-distrovars.inc'
        echo '# and ??= would be too weak to override it.'
        echo 'CONNECTIVITY_CHECK_URIS = ""'
        echo ""

        echo "# Build cache directories. User can override in local.conf if needed (e.g., NFS shared cache)."
        echo "DL_DIR ??= \"$WORKSPACE_DIR/downloads\""
        echo "SSTATE_DIR ??= \"$WORKSPACE_DIR/sstate-cache\""

        if [[ "$is_wsl" -eq 1 ]] && [[ -n "$parallel_n" ]]; then
            echo ""
            echo "# WSL parallelism auto-tuning (WSL swap is slower than bare-metal, prone to OOM)."
            echo "# Formula: N = max(1, min(nproc, (MemTotal_GB + SwapTotal_GB) / 4))"
            echo "# Override in local.conf if needed: PARALLEL_MAKE = \"-j 8\""
            echo "BB_NUMBER_THREADS ??= \"$parallel_n\""
            echo "PARALLEL_MAKE ??= \"-j${parallel_n}\""
        fi
    } > "$inc_file"

    mkdir -p "$WORKSPACE_DIR/downloads" "$WORKSPACE_DIR/sstate-cache"
```

  - 同时删除旧的 `has_dl_dir`/`has_sstate_dir`/`dl_dir_existing`/`sstate_dir_existing` 变量和对应的条件 mkdir 逻辑（约 1356-1411 行）

- [ ] Step 3: 验证语法
  - Run: `bash -n /home/iasi/ob-harness/ob && echo "syntax OK"`
  - Expected: `syntax OK`

---

### Task 3: 删除 ensure_bootstrap_local_conf() 中的 managed block 逻辑

- 目标：删除 `ensure_bootstrap_local_conf()` 中的 `# BEGIN/END ob init managed settings` block 写入和解析逻辑，保留 GITLAB_IP 检测 + include 行添加
- 涉及文件：`ob`（`ensure_bootstrap_local_conf()` 函数，`ob:397-569`）
- 验证范围：`ob init` 后 `local.conf` 不再包含 `# BEGIN ob init managed settings`

- [ ] Step 1: 确认要删除的代码范围
  - Run: `grep -n "managed_start\|managed_end\|managed_block\|CONNECTIVITY_CHECK_URIS\|OB_CONNECTIVITY_CHECK_URIS\|connectivity_value" /home/iasi/ob-harness/ob | head -15`
  - Expected: 确认 managed block 相关变量和逻辑集中在 `ob:399-546`

- [ ] Step 2: 删除 managed block 逻辑
  - Change: 在 `ensure_bootstrap_local_conf()` 中：
    1. 删除 `local connectivity_value="${OB_CONNECTIVITY_CHECK_URIS-}"` 变量（399 行）
    2. 删除 `connectivity_value` 的引号转义逻辑（407-409 行）
    3. 删除整个内嵌 Python 脚本（`bootstrap_output=$(python3 - ... PY)`），约 448-546 行
    4. 删除 `bootstrap_output` 解析逻辑（548-566 行）
    5. 保留 GITLAB_IP 检测（412-446 行）
    6. 保留最后的 `verbose` 行

  最终函数体应只包含：
  - local.conf 存在性检查
  - GITLAB_IP 检测（保留现有逻辑不变）
  - git insteadOf 配置（保留现有逻辑不变）
  - include 行添加逻辑（从旧 `generate_build_config()` 中的 1413-1424 行移入此处，因为 `ensure_bootstrap_local_conf` 在 Step 3 被调用，此时就能确保 include 行存在）
  - `verbose "Ensured ob init local.conf settings in $local_conf"`

  将 include 行添加逻辑从 `generate_build_config()` 移到 `ensure_bootstrap_local_conf()`：

```bash
    # Add include to local.conf if not already present
    local include_line="include externalsrc-$MACHINE.inc"

    if grep -qF "$include_line" "$local_conf"; then
        verbose "Include line already exists in local.conf, skipping."
    else
        cp "$local_conf" "${local_conf}.bak.$(date +%Y%m%d%H%M%S)"
        echo "" >> "$local_conf"
        echo "$include_line" >> "$local_conf"
        info "Added '$include_line' to local.conf"
    fi
```

- [ ] Step 3: 从 generate_build_config() 中删除 include 行添加逻辑和 DL_DIR/SSTATE_DIR 的 old-style 检测
  - Change: 删除 `generate_build_config()` 末尾的 include 行添加代码（原 1413-1424 行，现已在 Task 2 的重写中被包含，确保最终版本不包含这段重复逻辑）

- [ ] Step 4: 验证语法
  - Run: `bash -n /home/iasi/ob-harness/ob && echo "syntax OK"`
  - Expected: `syntax OK`

---

### Task 4: 集成验证（WSL 环境）

- 目标：在当前 WSL2 环境下端到端验证 `ob init` 的完整行为
- 涉及文件：`ob`、生成的 `.inc` 和 `local.conf`
- 验证范围：所有设计要求的行为都被满足

- [ ] Step 1: 运行 ob init 并观察输出
  - Run: `./ob init gb200nvl-obmc -v 2>&1 | grep -E "WSL|parallel|managed|CONNECTIVITY|BB_NUMBER|PARALLEL_MAKE|include|externalsrc"`
  - Expected: 看到 "WSL detected — parallelism limited to -jN" 提示，无 managed block 相关输出

- [ ] Step 2: 检查 .inc 内容
  - Run: `cat /home/iasi/ob-harness/workspace/openbmc/build/gb200nvl-obmc/conf/externalsrc-gb200nvl-obmc.inc`
  - Expected: 包含 `INHERIT += "externalsrc"`、`CONNECTIVITY_CHECK_URIS = ""`、`DL_DIR ??=`、`SSTATE_DIR ??=`、`BB_NUMBER_THREADS ??=`、`PARALLEL_MAKE ??=`

- [ ] Step 3: 检查 local.conf 不含 managed block
  - Run: `grep "BEGIN ob init\|END ob init\|CONNECTIVITY_CHECK_URIS" /home/iasi/ob-harness/workspace/openbmc/build/gb200nvl-obmc/conf/local.conf`
  - Expected: 无匹配（存量 block 不主动删除，但新生成的 local.conf 不应包含）

- [ ] Step 4: 验证公式值正确
  - Run: `echo "cores=$(nproc); mem_gb=$(( $(grep MemTotal /proc/meminfo | awk '{print $2}') / 1048576 )); swap_gb=$(( $(grep SwapTotal /proc/meminfo | awk '{print $2}') / 1048576 )); total=$(( mem_gb + swap_gb )); n=$(( total / 4 )); echo \"expected N=$n\""`
  - Expected: 输出的 N 值与 `.inc` 中 `BB_NUMBER_THREADS` / `PARALLEL_MAKE` 的值一致

- [ ] Step 5: 验证用户覆盖生效
  - Run:
    ```bash
    cd /home/iasi/ob-harness/workspace/openbmc
    set +u && source setup gb200nvl-obmc 2>/dev/null
    bitbake -e 2>/dev/null | grep "^PARALLEL_MAKE="
    ```
  - Expected: `PARALLEL_MAKE="-j<N>"` 其中 N 与公式计算值一致

- [ ] Step 6: checkpoint commit
  - Run: `git add ob && git commit -m "feat(ob init): WSL auto parallelism tuning + managed block migration"`
  - Expected: commit 成功

## 执行纪律

- 开始实现前先复查整份计划；如果发现缺项、矛盾、命名不一致或验证命令无效，先修计划
- 按任务顺序执行，不要无声跳步、合并步或改变任务目标
- 每完成一个任务，都运行该任务定义的验证
- 遇到阻塞、重复失败或计划与仓库现实不符，立即停下来说明，不要猜
- 当前在 `main` 分支；实现前先创建 feature branch
- 全部任务完成后，运行最终验证并输出修改摘要

## 最终验证

1. `bash -n ob` — 脚本语法无错误
2. `./ob init gb200nvl-obmc -v` — 正常完成，输出包含 WSL 检测提示
3. `.inc` 包含所有预期变量，操作符正确（`+=` / `=` / `??=`）
4. `local.conf` 不包含 managed block
5. `bitbake -e` 中 `PARALLEL_MAKE` 值符合公式

## 审阅 Checkpoint

实施计划已写好并保存到 `docs/plans/2026-06-07-ob-init-auto-parallelism-implementation-plan.md`。请先确认这份计划；如果没问题，下一步可以按计划由普通编码 agent 或人工继续执行。
